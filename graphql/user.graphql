type User{
    username: String!
    email: String!
    bio: String!
    image: String
}

type Query {
    getUsuarios: [User]
}

type Mutation {
    registerUsuario(
        username: String!
        email: String!
        hash: String!
        bio: String
        image: String
    ): User
}
